---
import { getCollection } from 'astro:content';
import Layout from '../../layouts/Layout.astro';
import Tagpage from '../../components/blog/Tagpage.astro';
import { ViewTransitions } from 'astro:transitions';

export async function getStaticPaths() {
    const paths = [];
    const blog = await getCollection('blog');
    const tags = [...new Set([].concat.apply([], blog.map(post => post.data.tags)))];
    const langPaths = tags.map((tag) => {
        return {
            params: { tag },
        };
    });
    paths.push(...langPaths);

    return paths;
}
---
<Layout comments={false}>
  <ViewTransitions />

  <nav class="tag-back" aria-label="breadcrumb">
    <a href="/tags" class="tag-back__link">
      <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
        <line x1="19" y1="12" x2="5" y2="12"/>
        <polyline points="12 19 5 12 12 5"/>
      </svg>
      <span>返回标签</span>
    </a>
  </nav>

  <Tagpage />
</Layout>

<style>
  .tag-back {
    padding: clamp(1.5rem, 4vw, 2.5rem) 0 0;
  }

  .tag-back__link {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.45rem 0.9rem;
    color: var(--gray);
    text-decoration: none;
    font-family: var(--font-family-mono);
    font-size: 0.78rem;
    letter-spacing: 0.05em;
    border: 1px solid var(--hairline);
    border-radius: 2rem;
    background: transparent;
    transition: color 0.3s ease, border-color 0.3s ease;
  }

  .tag-back__link svg {
    flex-shrink: 0;
    transition: transform 0.35s cubic-bezier(0.34, 1.56, 0.64, 1);
  }

  .tag-back__link:hover {
    color: var(--hc);
    border-color: rgba(125, 211, 252, 0.5);
  }

  .tag-back__link:hover svg {
    transform: translateX(-4px);
  }

  /* Match the tags shell width. */
  main { max-width: 1100px; }
</style>
</Layout>
