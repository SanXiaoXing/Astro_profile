---
title: Github Fork仓库更新日志
pubDate: 2024-12-27 20:35
description: fork失败经验总结
catalog: true
categories:
  - git
  - github
  - umani
tags:
  - git
  - github
  - umani
mathjax: true
Inv-tags: blog
pin: false
---
# 遇到问题
>因为前一段时间使用 `umani` 进行我的网站管理，但是我使用 GitHub + Vercel 的结合方式进行部署，随着时间的推移，umani 也进行对应的更新并且修复了很多 bug，以至于我邮箱内都是关于 `umani` 的项目报错信息。至此我想对此进行一个问题的汇总.

# 问题解决

当在 GitHub 上 fork 了别人的仓库后，原仓库（upstream）如果有更新，则需要手动同步这些更改到我的 fork 仓库。
## 添加上游仓库

- 在本地仓库中，运行命令添加上游仓库的 URL：

```bash
git remote add upstream https://github.com/umami-software/umami.git
```

## 确认远程仓库

- 检查是否正确添加了上游仓库：

```bash
git remote -v
```

- 输出内容为：

```plaintext
origin	git@github.com:SanXiaoXing/umami.git (fetch)
origin	git@github.com:SanXiaoXing/umami.git (push)
upstream	https://github.com/umami-software/umami.git (fetch)
upstream	https://github.com/umami-software/umami.git (push)
```

## 获取上游仓库更新

- 从上游仓库拉取最新更新：

```bash
git fetch upstream
```

## 合并上游分支更改到我的分支

- 切换到主分支

```bash
git checkout main
```

- 将上游的更改合并至主分支 （注意点）

```bash
git merge upstream/master
```

> 如果原仓库使用的是 `master` 分支，则将 `main` 替换为 `master`

- 如果不确定远程分支是否存在，可执行命令：
```bash
git branch -r
```

- 输出内容为： 
```plaintext
  origin/HEAD -> origin/main
  origin/main
  origin/master
  upstream/analytics
  upstream/dev
  upstream/master
  upstream/qa
  upstream/stage
  upstream/v1
(END)
```
## 提交内容

正常的进行 add 及相关的 cz 操作，最后 push 一下就行了